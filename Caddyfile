ecofinancesl.online, www.ecofinancesl.online {

    encode gzip

    # Backend API
    handle /api/* {
        reverse_proxy backend:5000
    }

    # Frontend
    handle {
        root * /srv/frontend
        file_server
    }
}